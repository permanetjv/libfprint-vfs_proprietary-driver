# Shared repository rules

This repository vendors the canonical rules from `permanet-pod/dev`. If the
include below is unavailable in a fresh clone or worktree, run:

```bash
git submodule update --init --recursive vendor/permanet-pod
```

Then read and follow:

@vendor/permanet-pod/RULES.md
